name: Modular

on:
  push:
    branches:
      - modular

jobs:
  all:
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            skip: []
            os: ubuntu-latest
            shell: "bash"
            ext: "sh"
          - name: macOS
            skip: ["lexical_cast"]
            os: macos-13
            shell: "bash"
            ext: "sh"
          - name: Windows
            skip: ["hof"]
            os: windows-2022
            shell: "cmd"
            ext: "bat"
    runs-on: ${{matrix.os}}
    defaults:
      run:
        shell: ${{matrix.shell}}
        working-directory: ${{github.workspace}}/boostroot
    steps:
      - name: "Checkout Boost"
        uses: actions/checkout@main
        with: { submodules: true, path: "boostroot" }
      - name: "Checkout B2"
        uses: actions/checkout@main
        with: { repository: "bfgroup/b2", path: "b2" }
      - name: B2
        working-directory: ${{github.workspace}}/b2
        run: ./bootstrap.${{matrix.ext}}
      - name: accumulators
        if: ${{ !contains(matrix.skip, 'accumulators') }}
        run: '"${{github.workspace}}/b2/b2" libs/accumulators//all'
      - name: callable_traits
        if: ${{ !contains(matrix.skip, 'callable_traits') }}
        run: '"${{github.workspace}}/b2/b2" libs/callable_traits//all'
      - name: compatibility
        if: ${{ !contains(matrix.skip, 'compatibility') }}
        run: '"${{github.workspace}}/b2/b2" libs/compatibility//all'
      - name: filesystem
        if: ${{ !contains(matrix.skip, 'filesystem') }}
        run: '"${{github.workspace}}/b2/b2" libs/filesystem//all'
      - name: hof
        if: ${{ !contains(matrix.skip, 'hof') }}
        run: '"${{github.workspace}}/b2/b2" libs/hof//all'
      - name: lexical_cast
        if: ${{ !contains(matrix.skip, 'lexical_cast') }}
        run: '"${{github.workspace}}/b2/b2" libs/lexical_cast//all'
      - name: mp11
        if: ${{ !contains(matrix.skip, 'mp11') }}
        run: '"${{github.workspace}}/b2/b2" libs/mp11//all'
      - name: predef
        if: ${{ !contains(matrix.skip, 'predef') }}
        run: '"${{github.workspace}}/b2/b2" libs/predef//all'
      - name: preprocessor
        if: ${{ !contains(matrix.skip, 'preprocessor') }}
        run: '"${{github.workspace}}/b2/b2" libs/preprocessor//all'
